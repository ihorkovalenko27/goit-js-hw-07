const countById = document.querySelector('#categories');
// console.log('В списке', countById.children.length, 'категории');

const searchTitle = document.querySelectorAll('.item h2');
// searchTitle.forEach(title => console.log('Категория:', title.textContent));

const countByCategories = document.querySelectorAll('.item ul');
// countByCategories.forEach(category =>
//   console.log('Количество элементов:', category.children.length),
// );
